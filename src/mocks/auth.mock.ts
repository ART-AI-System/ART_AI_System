// TODO: Replace mock data with API integration
import { MOCK_USER } from '../config/roles';

export const mockAuthUser = MOCK_USER;

export const mockLoginCredentials = {
  email: 'admin@artai.edu.vn',
  password: 'password123',
};
